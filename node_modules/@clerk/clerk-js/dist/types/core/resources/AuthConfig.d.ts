import type { AuthConfigJSON, AuthConfigResource } from '@clerk/types';
import { BaseResource } from './internal';
export declare class AuthConfig extends BaseResource implements AuthConfigResource {
    singleSessionMode: boolean;
    constructor(data: AuthConfigJSON);
    protected fromJSON(data: AuthConfigJSON | null): this;
}
