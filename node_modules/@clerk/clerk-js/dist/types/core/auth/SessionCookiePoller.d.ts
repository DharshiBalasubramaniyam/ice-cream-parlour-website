export declare class SessionCookiePoller {
    private lock;
    private workerTimers;
    private timerId;
    startPollingForSessionToken(cb: () => unknown): void;
    stopPollingForSessionToken(): void;
}
