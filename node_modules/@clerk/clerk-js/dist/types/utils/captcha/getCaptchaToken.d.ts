import type { CaptchaProvider, CaptchaWidgetType } from '@clerk/types';
type CaptchaOptions = {
    siteKey: string;
    scriptUrl: string;
    widgetType: CaptchaWidgetType;
    invisibleSiteKey: string;
    captchaProvider: CaptchaProvider;
};
export declare const getCaptchaToken: (captchaOptions: CaptchaOptions) => Promise<{
    captchaToken: string;
    captchaWidgetTypeUsed: string;
}>;
export {};
