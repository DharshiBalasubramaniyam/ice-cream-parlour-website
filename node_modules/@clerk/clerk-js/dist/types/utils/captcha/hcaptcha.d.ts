import type { CaptchaWidgetType } from '@clerk/types';
export declare const getHCaptchaToken: (captchaOptions: {
    siteKey: string;
    scriptUrl: string;
    widgetType: CaptchaWidgetType;
    invisibleSiteKey: string;
}) => Promise<{
    captchaToken: string;
    captchaWidgetTypeUsed: string;
}>;
