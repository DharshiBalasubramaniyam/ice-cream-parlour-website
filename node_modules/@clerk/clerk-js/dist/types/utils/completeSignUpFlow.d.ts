import type { SignUpResource } from '@clerk/types';
type CompleteSignUpFlowProps = {
    signUp: SignUpResource;
    verifyEmailPath?: string;
    verifyPhonePath?: string;
    navigate: (to: string) => Promise<unknown>;
    handleComplete?: () => Promise<void>;
    redirectUrl?: string;
    redirectUrlComplete?: string;
};
export declare const completeSignUpFlow: ({ signUp, verifyEmailPath, verifyPhonePath, navigate, handleComplete, redirectUrl, redirectUrlComplete, }: CompleteSignUpFlowProps) => Promise<unknown> | undefined;
export {};
