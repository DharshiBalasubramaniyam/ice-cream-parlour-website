import type { ClerkAPIError } from '@clerk/types';
export declare const VerificationErrorMessage: {
    Incorrect: string;
    CodeExpired: string;
};
export declare function verificationErrorMessage(err: Error): string;
export declare function isVerificationExpiredError(err: ClerkAPIError): boolean;
