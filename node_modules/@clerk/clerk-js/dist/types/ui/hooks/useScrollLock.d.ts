/**
 * Disables scroll for an element.
 * Adds extra padding to prevent layout shifting
 * caused by hiding the scrollbar.
 */
export declare const useScrollLock: <T extends HTMLElement>(el: T) => {
    disableScroll: () => void;
    enableScroll: () => void;
};
